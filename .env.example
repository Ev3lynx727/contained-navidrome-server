# Navidrome Configuration
# Copy this file to .env and customize as needed

# Music folder path (inside container)
ND_MUSICFOLDER=/music

# Data folder path (inside container)
ND_DATAFOLDER=/data

# Optional: Set admin password (leave empty for no password)
# ND_PASSWORD=your_secure_password

# Optional: Custom port (default: 4533)
# ND_PORT=4533

# Optional: Base URL for reverse proxy
# ND_BASEURL=

# Optional: Enable debug logging
# ND_LOGLEVEL=info

# Tailscale Configuration
# Get auth key from: https://login.tailscale.com/admin/settings/keys
# TS_AUTHKEY=tskey-auth-...